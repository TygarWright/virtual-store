"""Explicit business-domain boundary package for Virtual Store.

The legacy route modules remain compatible, while new code should place domain
rules here rather than importing Flask request/session objects directly.
"""
